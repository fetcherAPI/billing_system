import { Button1, ThemeButton } from 'shared/ui/Button1';

export const ActivateCompany = () => {
    return (
        <Button1 theme={ThemeButton.PRIMARY} style={{ alignSelf: 'flex-start' }}>
            Активировать
        </Button1>
    );
};
